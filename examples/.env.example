# Copy to .env and fill in your MplusKASSA API credentials.
# `export` keeps `source .env` working for child processes (see README).
export MPLUS_HOST=api.mpluskassa.nl
export MPLUS_PORT=port
export MPLUS_IDENT=your-ident
export MPLUS_SECRET=your-secret
