## What & why

<!-- One paragraph. Link the issue if there is one. -->

## Checklist

- [ ] Tests added/updated (generator changes: pinned in `test/generator.test.ts`)
- [ ] `npm run check` and `npm run lint` pass locally
- [ ] No manual edits to `src/generated/` (regenerated via `scripts/generate.ts`)
